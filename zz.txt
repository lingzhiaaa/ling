1 让定时器里面的函数立即执行，可以用setInterval((function(){})(),100),
	这样他就不会管他延迟多少秒，立即执行，但他也脱离了定时器，不会都是执行了，只会执行一次；

2	结束动画
	$("#dv").finish() , 瞬间返回起点
	。stop(boolean,boolean) 1个表示后续动画是否执行，true表示不执行；2参表示是否立即完成当前
动画，true瞬间完成，false立即停止；什么都不加默认都是false;